apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "utilities-billing.fullname" . }}-test-connection"
  labels:
    {{- include "utilities-billing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "utilities-billing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
